import sys
import json

def home(filter):
    return json.dumps([])

def category(tid, pg, filter):
    return json.dumps([])

def detail(ids):
    return json.dumps([])

def search(key, quick):
    return json.dumps([])

def player(flag, id):
    return json.dumps({})